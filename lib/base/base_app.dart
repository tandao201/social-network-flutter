import 'package:flutter/material.dart';


class BaseApp extends StatelessWidget {
  final Widget child;

  const BaseApp({Key? key, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }

}